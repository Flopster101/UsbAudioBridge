<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.UsbAudioMonitor" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <!-- M3 uses dynamic colors by default, but we can override specifics if needed. -->
        <!-- For now, we rely on Dynamic Colors (Monet) applied in Activity. -->
    </style>
</resources>
